//WAP to accept number and print the summation and factorial
#include<stdio.h>
int main()
{
	int num,i,sum=0;
	printf("Enter the number:");
	scanf("%d",&num);
	for(i=sum;i<=num;sum=sum+i,i++);
	printf("%d",sum);
	
}
